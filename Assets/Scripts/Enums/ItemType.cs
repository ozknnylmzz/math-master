namespace Math.Enums
{
    public enum ItemType
    {
        None = -1,
        BoardItem = 0,
        OperatorItem=1,
    }
    public enum MatchDetectorType
    {
        Horizontal,
        Vertical,
    } 
}